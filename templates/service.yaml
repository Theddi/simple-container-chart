apiVersion: v1
kind: Service
metadata:
  name: {{ include "simple-container.fullname" . }}
  labels:
    {{- include "simple-container.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "simple-container.selectorLabels" . | nindent 4 }}
